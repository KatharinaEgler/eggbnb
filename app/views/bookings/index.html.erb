<h1> Your bookings</h1>

<ul>
  <% @bookings.each do |booking| %>
    <li>
      <%= link_to booking_path(booking) do %>
       <p><%= booking.user.name%>  <%= booking.start_date.to_s%> - <%=booking.end_date.to_s%></p>
      <% end %>
    </li>
  <% end %>
</ul>
